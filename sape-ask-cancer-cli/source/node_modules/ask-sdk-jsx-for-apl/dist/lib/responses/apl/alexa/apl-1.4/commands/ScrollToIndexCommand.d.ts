import { LiteralUnion } from '../../../common';
declare type alignEnum = 'first' | 'center' | 'last' | 'visible';
export interface IScrollToIndexCommand {
    description?: string;
    delay?: number | string;
    screenLock?: boolean | string;
    sequencer?: string;
    when?: boolean | string;
    align?: LiteralUnion<alignEnum, string>;
    componentId?: string;
    index: number | string;
    [key: string]: unknown;
    readonly type: 'ScrollToIndex';
}
export {};
