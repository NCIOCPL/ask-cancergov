import { LiteralUnion } from '../../../common';
declare type audioTrackEnum = 'foreground' | 'background' | 'none' | 'mute';
export interface IPlayMediaCommand {
    description?: string;
    delay?: number | string;
    screenLock?: boolean | string;
    sequencer?: string;
    when?: boolean | string;
    audioTrack?: LiteralUnion<audioTrackEnum, string>;
    componentId?: string;
    source: string;
    [key: string]: unknown;
    readonly type: 'PlayMedia';
}
export {};
