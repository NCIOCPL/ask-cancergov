/// <reference types="react" />
import { SkillResponsePart } from '../../skill-response';
import { Command } from '../alexa/apl-1.4/commands';
import { ImportDefinition, MainTemplateDefinition, LayoutDefinition, ExtensionDefinition } from '../common/context';
import { ResponseBuilder } from 'ask-sdk-core';
export interface APLDocument {
    type: 'APL';
    settings?: object;
    version: string;
    description?: string;
    background?: string;
    import?: ImportDefinition[];
    theme: string;
    layouts?: LayoutDefinition;
    extensions?: ExtensionDefinition[];
    commands?: object;
    onMount?: Command[];
    handleKeyDown?: object[];
    handleKeyUp?: object[];
    resources?: object[];
    styles?: object;
    graphics?: object;
    export?: object;
    mainTemplate: {
        parameters: string[];
        items: unknown[];
    };
}
export interface APLDirective {
    type: 'Alexa.Presentation.APL.RenderDocument';
    token?: string;
    document: APLDocument;
    datasources?: object;
}
interface APLProps {
    theme?: 'light' | 'dark';
    settings?: object;
    description?: string;
    background?: string;
    dataSources?: object;
    token?: string;
    layouts?: LayoutDefinition;
    import?: ImportDefinition[];
    extensions?: ExtensionDefinition[];
    commands?: object;
    onMount?: Command[];
    handleKeyDown?: object[];
    handleKeyUp?: object[];
    resources?: object[];
    styles?: object;
    export?: object;
    graphics?: object;
}
export declare class APL extends SkillResponsePart<APLProps> {
    private static packageVersion;
    private layouts;
    readonly items: unknown[];
    readonly imports: ImportDefinition[];
    readonly mainTemplate: MainTemplateDefinition;
    readonly directive: APLDirective;
    protected register(responseBuilder: ResponseBuilder): void;
    private unifyDocumentFields;
    private removeEmptyDocumentFields;
    private registerUserAgent;
    render(): JSX.Element;
}
export {};
